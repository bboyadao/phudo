version: '2'
services:
  nginx:
    container_name: 'nginx'
    image: nginx:latest
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./janus-gateway/html:/root/janus-gateway/html/
      # - /etc/letsencrypt:/etc/letsencrypt:ro

    networks:
      - youbook_network
    # depends_on:

  janusgw:
    container_name: 'janusgw'
    build: ./
    ports:
      # - "80:80"
      - "7088:7088"
      - "8088:8088"
      - "8188:8188"
      - "8089:8089"
      - "10000-10200:10000-10200/udp"
    environment:
      - DOCKER_IP=192.168.2.103

    networks:
      - youbook_network


volumes:
  janus_static:

networks:
  youbook_network:
    driver: bridge
